name: Generate file every 10 minutes

on:
  schedule:
    - cron: "*/10 * * * *"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Create file
        run: |
          mkdir -p output
          echo "Generated at $(date)" > output/file.txt

      - name: Commit and push
        run: |
          git config user.name "github-actions"
          git config user.email "github-actions@github.com"
          git add output/file.txt
          git commit -m "Auto update file" || echo "No changes"
          git push
